# Rackpad v1.8.3-beta.0 combined candidate

Beta candidate combining maintenance, stabilization, stacked switches, and SNMPv3 repairs.
The intended stable release is **1.8.3**, after acceptance. Docker remains the
supported general deployment; native Proxmox LXC remains experimental.

## Changes

- SNMPv3 RFC key localization and AES128 privacy encoding, authenticated response
  validation, correlated discovery, bounded engine-time synchronization, and
  authenticated trap security levels/timeliness. Stored credentials are retained.
- Focused Chromium/Firefox Storage regressions for unavailable/throwing UUID APIs
  on fresh and schema-50-upgraded data, plus real Net-SNMP interoperability in CI.

- Ordered stacked-switch members, metadata and labelled MAC addresses, nullable
  canonical port assignments, derived physical layouts, and lab-scoped member APIs.
  Existing ordinary switches and their port/link identities remain unchanged.
- Rack Studio unmount, undo, redo, and loose-room moves return canonical placement
  state while retaining the stack's derived height in storage. Genuine concurrent
  changes still return a conflict.
- Mixed-face/rear cable continuation rendering and exports, plus deterministic
  documentation screenshots required after application checks in reusable quality.
  Screenshot raster settings preserve automatic frames on Linux; the inherited
  manual-frame flag was reproduced stalling readiness and removed.
- Docker installer uses the channel-aligned release manifest, preserves existing
  configuration and keys, quotes supplied keys exactly, and verifies Compose key
  readback before pulling images or replacing configuration. Custom Compose files
  receive a proposal; recognized managed files receive a protected backup.
- Native readiness uses a shared 60-second deadline for bounded systemd and HTTP
  calls, strict health JSON, and post-readiness authentication, SPA, and collector
  checks. Failed activation restores the paired application, database and config.
- Publication requires quality, CodeQL findings, and security gates; tagged releases
  consume this notes file. Operator guidance and dependency grouping are refreshed.

The existing stack interfaces are retained. Remediation adds no further API or
environment-variable names and does not change deployment exposure defaults.

## Upgrade and recovery

The candidate starts at published beta.5 commit
`767d50764a3e0173ca71cdb6c56d342a43a7ccb5`. Published migrations **49 and 50 are
unchanged**; stack migration **51** is appended. Schema and restore manifests use
51. Security conversion remains at the schema-50 boundary, including old-backup
conversion. Repeat startup at 51 must leave data unchanged.

Before upgrading, save the previous application artifact and a matching database,
configuration and encryption-key snapshot. Preserve `RACKPAD_SECRET_KEY` exactly;
losing it makes existing encrypted credentials unreadable. Missing-key conversion
of legacy inline communities fails atomically. See the retained
[security upgrade instructions](v1.8.2-beta.4-test-notes.md) for trusted proxy IPs,
OIDC re-login and role recalculation, trap-source reconfiguration, and community
rotation. Traps remain explicitly opt-in.

Rollback restores the **previous application with its pre-upgrade database and
configuration**. Older binaries must never open schema-51 data. A current logical
export cannot substitute for the pre-upgrade database snapshot when downgrading.
Logical and native restores must reject newer schemas or invalid ownership without
changing the destination. Keep backup retention and original keys unchanged.

## Exact-candidate acceptance checklist

Run against the immutable candidate commit and published artifacts. Record
commit, package version, image digests for both architectures, source-asset hashes,
guest OS, PVE version, discovery mode, timestamps, and pass/fail evidence together.
A changing branch or floating image tag is insufficient evidence. Local containers and hosted CI do not substitute for real guest acceptance.

For **each** disposable Debian 13 and Ubuntu 24.04 guest on Proxmox VE 9:

- [ ] Fresh install on the default port; confirm startup, first-admin bootstrap,
      login, authentication denial, SPA deep links, fonts/assets and collectors.
- [ ] Upgrade preserved synthetic schema 45, 48, 49 and 50 datasets to 51; verify
      member, port and link identities, secrets, OIDC recovery and repeat startup.
- [ ] Reboot after install and upgrade; verify the same application/data pairing,
      service readiness and persistent configuration.
- [ ] Repeat on a custom port; check origin/host validation, HTTP endpoints and
      advertised access information.
- [ ] Exercise safe discovery, explicit advanced opt-in, insufficient-capability
      refusal, and separately opted-in traps. No outer privilege changes.
- [ ] Verify generated, supplied and existing quoted keys, including ` #` and
      leading/trailing spaces. Unsupported inputs fail without replacement or
      secret output. Confirm keys survive update, reboot, restore and rollback.
- [ ] Logical and native backup/restore round trips preserve ownership, grants,
      stack members/MACs/height, ports and links. Invalid/newer snapshots fail
      atomically. Restore only disposable data.
- [ ] Inject delayed startup, systemd and HTTP stalls, deadline-edge completion,
      malformed/misleading health, service exit, endpoint failure, snapshot failure
      and activation failure. Verify bounded failure and complete paired rollback.
- [ ] After rollback and reboot, confirm the previous version, original schema,
      database/configuration bytes, encryption key, login and collectors agree.

Community acceptance on that same candidate:

- [ ] Cisco IOS-XE (#152): record IOS-XE and host/container versions plus candidate
      digest. From the same Docker bridge used by Rackpad, confirm a read-only
      Net-SNMP SHA/AES query succeeds. Configure the same credential in Rackpad;
      verify credential testing, repeated monitoring and IF-MIB discovery/import.
      Check interface/port identities after repeated discovery. Repeat after an
      idle period beyond 150 seconds and an operator-approved device restart or
      engine-time reset. Wrong credentials must fail without unsigned fallback.
      Record outcomes and sanitized errors; never post passwords or raw captures.
- [ ] Firefox Storage (#153): record Firefox version and candidate digest. Open
      the disposable installation over ordinary LAN HTTP, confirm whether
      `crypto.randomUUID` is available, and navigate all Storage tabs. Create a
      custom template with two sections, save independent section edits, reload,
      and confirm persistence. Repeat with upgraded storage inventory and pools.
      Retain console-error assertions; also confirm Chromium behavior.

- [ ] Rear and mixed-face routing (#139): continuation markers, selections, traces,
      waypoints, screen rendering and exports on both faces.
- [ ] Patch panels (#148): both starter faces, independent face edits, 48 bindings
      with zero unmapped ports, and 24 pass-through pairs for a custom 24-column
      descendant. Confirm authoring and tracing across both faces.
- [ ] Stacked switches (#146): create/edit/reorder members and MACs; assigned and
      stack-wide ports; inherited custom types; all placement modes; resize and
      ownership conflicts; unmount/undo/redo, loose-room moves and earlier history;
      roles/cross-lab denial; integration refresh and backup/restore identity.
- [ ] Seven consecutive days of soak after all required acceptance passes. Record
      start/end times, daily observations and failures. Repeat affected acceptance
      and restart soak after a material change.

## Validation evidence

The table below records the initial combined candidate before the follow-up
SNMP repair. It is historical validation, not proof of the changed candidate.
Follow-up results must be recorded separately after execution. Earlier unpublished
[maintenance](v1.8.2-beta.6.md), [stabilization](v1.8.2-beta.6-test-notes.md), and
[stack](v1.9.0-dev.1-test-notes.md) evidence is historical and does not constitute
validation of this candidate. Published beta.5 release evidence is preserved.

Executed on 8 September 2026 using host Node 22.22.3 and Playwright 1.61.1;
the Linux container uses Node 22.23.2:

| Check | Combined-candidate result |
| --- | --- |
| Worktree dependency installation | `npm ci` completed locally; no external `node_modules` symlink |
| Font preflight | All seven IBM Plex font faces loaded successfully before the browser matrix |
| Standard application gate | Passed: lint/proof, all typechecks, i18n, 308 server tests, 92 client tests, configuration/docs contracts, production build and bundle budget |
| Installer regression suite | 26 passed; includes supplied whitespace/` #`, quoted existing keys, unsupported inputs, mismatched readback and malformed `.env` secret suppression |
| Native fixtures | 30 passed, retaining both suites plus the post-systemd deadline edge |
| Full browser matrix | `check:full` passed: 39 browser tests in 15.2 minutes, including the complete 432-case accessibility matrix, stack workflow, patch-panel authoring and OIDC browser security |
| Screenshot determinism | Passed on macOS host and isolated Linux arm64: two captures of all 40 images on each platform, zero changed pixels and maximum channel delta 0; thresholds unchanged |
| Bash / ShellCheck / actionlint | Passed for all 13 shell scripts and GitHub workflows |
| PowerShell syntax | Passed for the collector using a disposable official PowerShell 7.5 Ubuntu 24.04 container |
| Compose | All three shipped manifests rendered successfully with a synthetic retained key |
| Dependency audit | Zero npm vulnerabilities, including development dependencies |
| Filesystem scan | Trivy: zero high/critical dependency, secret or configuration findings in source/manifests; generated files and installed modules excluded |
| Image builds | Both `linux/amd64` and `linux/arm64` built and loaded locally; no push |
| Container smoke | Both architectures passed health, bootstrap/login and auth denial, SPA/CSP, six JS/CSS assets, seven fonts, logical round trip, offline native restore and repeat startup at 51 |
| Independent review | Migration/restore/auth review: 62 passing tests/subtests; stack/history review: 15 passed; installer/native review: 26 and 30 passed; no remaining actionable findings |
| Preservation | All four source worktrees retain their starting HEAD/status and all 249 pending-path hashes or deletions; no duplicate copies, databases, backups, secrets or generated output enter the candidate diff |

Both architecture-specific image scans report **51 high and 3 critical OS-package
advisories, with zero fixed versions available**; the fixable high/critical gate
passes. Node-package scanning reports no high/critical findings. The critical
entries are `CVE-2026-13221`, `CVE-2026-42496` and `CVE-2026-8376` in `perl-base`.
These are reported, not newly suppressed. Rebuild and rescan before publication;
zero fixable findings is not a claim of zero vulnerabilities.

The local multi-architecture image index is
`sha256:9dda15f0e14f9f143bef26aecb9812f508754a43de55559474c5eb543728a7d4`.
Architecture manifests: amd64
`sha256:e965629f743c4e2a4f32289a60d048b8e3000030eae2be245c77c2375c22502e`,
arm64 `sha256:8a43c70e484f3ffb58564cb70bf7b723977e5cffbde36a2c458d911fdd371418`.
Container smoke used non-root execution, read-only root filesystems, dropped
capabilities, no-new-privileges and network isolation, with newly created test
volumes removed afterward. The full application gate uses temporary databases.

Real guest acceptance, community acceptance and the seven-day soak remain unrun.
Live publication-gate failure behavior also requires CI evidence. Local workflow
contract tests do not prove hosted branch protection or release behavior.

## Follow-up remediation validation

The SNMP/Storage follow-up adds independent RFC wire fixtures and a real Net-SNMP
peer, covering the protocol failures missed by the initial suite. Executed on
8 September 2026:

- All 344 server and 92 client tests passed; 46 focused SNMP/egress/trap checks
  cover independent key vectors, response integrity/correlation, report retries,
  stale/concurrent engine state and trap security/timeliness.
- Real Net-SNMP SHA/MD5 with authNoPriv/AES128 passed GET/GETNEXT, credential API
  testing, monitoring, concurrent IF-MIB walks and incoming authenticated traps
  on an isolated Docker bridge with non-root fixtures and no published ports.
- All 12 focused Chromium/Firefox Storage cases passed on fresh and schema-50
  upgrade fixtures, including native, missing and throwing UUID APIs.
- Bash/ShellCheck (13 scripts), actionlint, PowerShell syntax (one collector),
  all three Compose renders, npm audit and filesystem scanning passed. npm
  reports zero vulnerabilities; filesystem high/critical vulnerability, secret
  and configuration findings are zero.
- Both architecture images built and passed health, bootstrap/login/auth denial,
  SPA/CSP/assets/fonts, logical restore, native restore and repeated startup at 51.
  Each architecture-specific image scan reports 51 high and 3 critical upstream
  OS advisories, with zero fixable findings and zero high/critical Node findings.
- Independent SNMP review identified clock and trap timing defects; each was
  reproduced and fixed with regressions. Separate migration/auth/installer/native
  review found no actionable defect; its UDP-restricted test gaps are covered by
  the full server run with local transport access.
- macOS and Linux screenshot determinism passed for two captures of all 40
  images on each platform, with zero changed pixels and maximum channel delta 0.
  The focused Storage suite also passed all 12 cases in Linux.
- `check:full` passed: lint/proof, typechecks, i18n, configuration/installer/native
  and documentation contracts, production build/bundle, and all 42 Chromium
  browser tests in 14.5 minutes, including the complete 432-case accessibility
  matrix. All seven font faces loaded before the long browser matrix.
- All four original worktrees and their 249 pending-path hashes/deletions remain
  unchanged. The final staged diff passed its whitespace and artifact checks.

The follow-up local multi-architecture image index is
`sha256:cbb94d24d5804cf85fab6d37590e2083d2eb8c165dbcd2f192c5b354c0be6920`.
Architecture manifests: amd64
`sha256:7dcd16ae19a6f3f479949cf8801813f62db73335088130160ff11a6652c3befc`,
arm64 `sha256:3575fea4f62a0ff22a832f01208755d6d22446d69f2fa5e0e8aef1c693ae3654`.
These are local validation artifacts; published digests must be captured and
smoke-tested separately before community acceptance begins.

## Publication-gate remediation

Hosted quality at commit `8fa95c3e5a180f74d3797ef8932836ae09870719` passed the
full application gate, Chromium/Firefox Storage cases, real Net-SNMP
interoperability, screenshot determinism, and shell/PowerShell/workflow checks.
Hosted filesystem/image scans also passed. CodeQL reported two high-severity
password-hashing findings at the RFC 3414 wire-key operations and prevented the
image build and release jobs from running.

The reviewed exception policy is limited to those two exact locations and binds
the source-file hash, unchanged server tree, owner and expiry at
**2026-11-30 00:00 UTC**. Independent protocol review established that the hashes
are required for configured SNMP interoperability; password storage remains
separate. Changed source or missing evidence disables the exceptions. Dates and
source identifiers must never be renewed automatically.

Raw CodeQL analysis remains complete and is retained with an explicit review
summary. Only eligible findings may be omitted from the separate uploaded report.
Malformed analysis, additional blockers and report-upload failures stop
publication; policy failures upload the raw results. No repository-wide hashing
exclusion or severity reduction is introduced.

Independent review found and reproduced malformed-report filtering and stale
summary defects in the initial policy implementation. The fixes validate complete
raw input against the pinned official SARIF schema and use separate evidence for
every invocation. The validator is development tooling; runtime SNMP code is
unchanged. All 12 publication regressions and actionlint passed, and the reviewer
confirmed both defects no longer reproduce.

After those repairs, clean `npm ci` and the standard application gate passed:
344 server tests, 92 client tests, 46 configuration/publication/installer cases,
30 native fixtures, documentation contracts, lint/type checks and production
build/bundle validation. npm audit found zero vulnerabilities. Runtime dependency
name/version sets are unchanged by the development-only validator additions.

The GitHub API-exported report identifies the two reviewed locations but contains
schema-invalid transformed fields, so it cannot substitute for native raw CodeQL
output. The changed workflow must pass against freshly generated hosted output
before publication; invalid reports remain blocking. Earlier green quality runs
do not establish that the new reporting workflow works on GitHub.

At `536ee0df74b260220e81e9350a0a432bb5751efd`, the native report passed schema
validation but omitted its absolute `%SRCROOT%` mapping, so both findings stayed
blocking and raw analysis was uploaded. The workflow now requests complete
analyzed file contents using CodeQL's `--sarif-add-file-contents` option. When
that mapping is absent, exceptions additionally require the indexed artifact's
complete UTF-8 content to match the approved SHA-256. An unresolved placeholder
alone remains insufficient; missing, duplicate or conflicting source evidence
still blocks. Analysis and upload explicitly use the same checkout root.
All 13 publication tests, actionlint and the standard application gate passed
after this repair. Independent review found no actionable remaining defect in
the evidence handling. Fresh native hosted analysis and upload remain required.

The local amd64 and arm64 rehearsal images rebuilt with refreshed runtime
packages passed authentication/denial, SPA/assets/fonts, logical/native recovery,
stack identity and repeated schema-51 startup checks using disposable data.
Both scans reported zero fixable high/critical or Node dependency findings and
54 unfixed upstream OS advisories (51 high, 3 critical) per architecture. These
local results do not replace verification of the immutable published digests.

## Remaining acceptance and issue updates

Community testers provide the Cisco and Proxmox results. Main requires Rackpad's
installer/feature acceptance and seven consecutive days of soak. The external
Community Scripts listing remains tracked in #138 and does not block main.
All six issues (#138, #139, #146, #148, #152 and #153) remain open, including after
acceptance and publication; updates record evidence without closing them.
Unfixed upstream OS advisories
are documented; fixable high/critical findings block release. Material changes
repeat affected acceptance and restart soak.

## Publication boundary

The approved release plan prepares the reviewed candidate and beta publication
through its normal PR and tag workflows. Main/stable promotion remains gated on
community acceptance, seven consecutive days of soak, final checks, and the
applicable publication authorization.
Unrelated open PRs are excluded. Existing worktrees and duplicate files remain
untouched. Promote to stable **1.8.3** only after acceptance, soak and final release
checks pass on the exact candidate.
